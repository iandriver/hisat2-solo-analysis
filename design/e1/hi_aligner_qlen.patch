--- /Users/iandriver/Downloads/hisat2/hi_aligner.h	2026-08-04 14:48:24
+++ /private/tmp/claude-501/-Users-iandriver-Downloads-hisat2/2400b599-e2d4-4d43-95ad-d6f519cf1f37/scratchpad/e1/hi_aligner.h	2026-08-17 12:17:06
@@ -23,6 +23,7 @@
 #include <iostream>
 #include <utility>
 #include <limits>
+#include "qlen_probe.h"
 #include "qual.h"
 #include "ds.h"
 #include "sstring.h"
@@ -6410,6 +6411,7 @@
                                 fw,
                                 (index_t)offset,
                                 (index_t)(cur - offset));
+        qlen_record((unsigned long)(cur - offset));
         hit.done(true);
 		return 0;
     }
@@ -6427,6 +6429,7 @@
                                     fw,
                                     (index_t)offset,
                                     (index_t)(cur - offset));
+        qlen_record((unsigned long)(cur - offset));
             if(cur >= hit._len) {
                 hit.done(true);
             }
@@ -6448,6 +6451,7 @@
                                 fw,
                                 (index_t)offset,
                                 (index_t)(cur - offset));
+        qlen_record((unsigned long)(cur - offset));
         if(cur >= hit._len) {
             hit.done(true);
         }
@@ -6643,6 +6647,7 @@
     assert_gt(left, 0);
     if(left < ftabLen + 1) {
         hitlen = left;
+        qlen_record((unsigned long)hitlen);
         return 0;
     }
     
@@ -6651,6 +6656,7 @@
         int c = seq[len-dep-1-i];
         if(c > 3) {
             hitlen = (i+1);
+            qlen_record((unsigned long)hitlen);
             return 0;
         }
     }
@@ -6660,6 +6666,7 @@
     dep += ftabLen;
     if(range.first >= range.second) {
         hitlen = ftabLen;
+        qlen_record((unsigned long)hitlen);
         return 0;
     }
     
@@ -6739,6 +6746,7 @@
         node_top = node_range.first; node_bot = node_range.second;
         nelt += (node_bot - node_top);
         hitlen = dep - offset;
+        qlen_record((unsigned long)hitlen);
     }
     return (index_t)nelt;
 }
@@ -6792,6 +6800,7 @@
     assert_gt(left, 0);
     if(left < ftabLen + 1) {
         hitlen = left;
+        qlen_record_local((unsigned long)hitlen);
 		return 0;
     }
     // Does N interfere with use of Ftab?
@@ -6807,6 +6816,7 @@
     dep += ftabLen;
     if(range.first >= range.second) {
         hitlen = ftabLen;
+        qlen_record_local((unsigned long)hitlen);
         return 0;
     }
     LOCAL_INIT_LOCS(range.first, range.second, tloc, bloc, gfm);
@@ -6886,6 +6896,7 @@
         node_top = node_range.first; node_bot = node_range.second;
         nelt += (node_bot - node_top);
         hitlen = dep - offset;
+        qlen_record_local((unsigned long)hitlen);
     }
 
     return (index_t)nelt;
